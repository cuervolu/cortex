meta {
  name: Get All Roadmaps
  type: http
  seq: 7
}

get {
  url: {{BASEURL}}/api/v1/education/roadmap/admin?sort=title
  body: none
  auth: bearer
}

params:query {
  sort: title
}

auth:bearer {
  token: {{token}}
}
