meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BASEURL}}/api/v1/auth/authenticate
  body: json
  auth: none
}

body:json {
  {
    "username": "cuervolu",
    "password": "{{PASSWORD}}"
  }
}
