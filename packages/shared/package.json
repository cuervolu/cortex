{
  "name": "@cortex/shared",
  "version": "0.1.0",
  "description": "Shared code for Cortex",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@nuxt/kit": "^3.13.0"
  }
}
